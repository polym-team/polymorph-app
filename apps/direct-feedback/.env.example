# DirectFeedback 백엔드 앱 환경변수

# 자체 MySQL (Prisma). 실제 값은 apps/direct-feedback/.env 에 두며 git 에 커밋하지 않는다.
DATABASE_URL="mysql://user:password@localhost:3306/directfeedback"

# 중앙 인증 서버 (oauth-server). 로컬은 http://localhost:3007
NEXT_PUBLIC_OAUTH_SERVER_URL="https://oauth.polymorph.co.kr"
